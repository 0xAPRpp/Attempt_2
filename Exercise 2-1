/*Write a program that will compute the area of a circle.The program
should prompt for the radius of the circle to be entered from the keyboard, calculate the
area using the formula area = pi * radius* radius, and then display the result.*/

# include <iostream>
# include <iomanip>

const long double value_of_pi{ 3.141592653589793238 };

int main()
{
	long double radius{};
	std::cout << "Enter Radius: " << std::endl;
	std::cin >> radius;

	std::cout << "Area is: " << std::fixed << std::setprecision(18) << value_of_pi * radius * radius << std::endl;

}
