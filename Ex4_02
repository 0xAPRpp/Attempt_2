# include <iostream>
# include <iomanip>

int main()
{
	int x{};

	std::cin >> x;

	if (x > 50)
	{
		if (x < 100)
			std::cout << "Correct Value" << std::endl;
		else
			std::cout << "Incorrect Value" << std::endl;
	}
	else
		std::cout << "Incorrect Value" << std::endl;

	system("pause");
}
